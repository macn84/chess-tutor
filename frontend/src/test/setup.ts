/**
 * Vitest global test setup.
 *
 * Imports @testing-library/jest-dom so all tests get DOM matchers like
 * toBeInTheDocument(), toHaveTextContent(), etc. without explicit imports.
 */

import '@testing-library/jest-dom'
